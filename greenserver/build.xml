<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<!--
  build.xml - The Green solver build script

  Public targets:
    build (default)   compile classes
    test              run all unit tests
    clean             remove the files generated by the build process
    package           create a jar file of the project
-->

<project name="greenserver" default="build" basedir=".">

<!--
    Settings
-->

    <property environment="env"/>
    <property name="output.dir" value="bin"/>
    <property name="junit.dir" value="bin/junit"/>
    <property name="jar.file" value="greenserver.jar"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="1.6"/>
    <property name="source" value="1.6"/>
    <path id="greenserver.classpath">
        <pathelement location="${output.dir}"/>
        <pathelement location="lib/green.jar"/>
    </path>

<!--
    Initialization
-->

    <target name="init">
        <mkdir dir="${output.dir}"/>
        <copy includeemptydirs="false" todir="${output.dir}">
            <fileset dir="src">
                <exclude name="**/*.java"/>
            </fileset>
            <fileset dir="C"/>
        </copy>
		<!--
        <copy includeemptydirs="false" todir="${output.dir}">
            <fileset dir="test">
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
		-->
    </target>

<!--
    TARGET: clean
-->

    <target name="clean">
        <delete dir="${junit.dir}"/>
        <delete dir="${output.dir}"/>
        <delete file="${jar.file}"/>
    </target>

<!--
    TARGET: build
-->

    <target name="build" depends="init">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="${output.dir}" source="${source}" target="${target}" includeantruntime="false">
            <src path="src"/>
            <classpath refid="greenserver.classpath"/>
        </javac>
		<!--
        <javac debug="true" debuglevel="${debuglevel}" destdir="${output.dir}" source="${source}" target="${target}" includeantruntime="false">
            <src path="test"/>
            <classpath refid="greenserver.classpath"/>
        </javac>
		-->
    </target>

<!--
    TARGET: test
-->

	<!--
    <target name="test">
        <mkdir dir="${junit.dir}"/>
        <junit fork="yes" printsummary="withOutAndErr">
            <formatter type="xml"/>
            <test name="za.ac.sun.cs.greenserver.EntireSuite" todir="${junit.dir}"/>
            <env key="DYLD_LIBRARY_PATH" value="lib/macosx"/>
            <env key="LD_LIBRARY_PATH" value="lib/linux"/>
            <classpath refid="greenserver.classpath"/>
        </junit>
        <junitreport todir="${junit.dir}">
            <fileset dir="${junit.dir}">
                <include name="TEST-*.xml"/>
            </fileset>
            <report format="frames" todir="${junit.dir}"/>
        </junitreport>
    </target>
	-->

<!--
    TARGET: package
-->

    <target name="package">
        <jar destfile="${jar.file}">
            <fileset dir="${output.dir}">
                <exclude name="${junit.dir}"/>
            </fileset>
            <fileset dir="src"/>
            <fileset dir="C"/>
        </jar>
    </target>

</project>
